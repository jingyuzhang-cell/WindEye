# Tests

This directory separates validation scripts from production code.

- `legacy/test_system_v1.py`
- `legacy/test_system_v2.py`

They are preserved for historical regression checks and still depend on the legacy prototype layout under `backend/new_report/code/legacy`.
